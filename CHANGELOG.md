# Changelog

本项目采用 `v主版本.次版本.修订版本` 标签。首次对外分发从 `v0.18.7` 开始；更早版本属于内部开发历史，不应上传或分发。

## v0.18.8 — 2026-09-07

### 修复

- 移动端浏览器回归测试会等待应用外壳真正挂载，不再因 GitHub 托管运行器启动较慢而提前访问空元素；
- ZIP 更新器的旧版 systemd 配置测试会保留当前 Node.js 可执行目录，兼容 GitHub `setup-node` 不将 Node 安装到 `/usr/bin` 的环境；
- 发布检查现在可以在服务器和 GitHub Actions 的 Node.js 22 环境中得到一致结果；
- 新增面向仓库专用 Deploy Key 的隔离镜像发布脚本，只接受已清洗 ZIP，且以原子操作推送 `main` 和版本标签；
- 新标签会在 GitHub Actions 再次通过完整检查后自动生成 ZIP、clean Git bundle、SHA-256 校验文件并创建 Release。

## v0.18.7 — 2026-09-06

首个隐私清洗后的组内发布候选版。

### 主要能力

- 手机优先的 Codex 任务列表、历史记录、实时输出和跨端平滑交接；
- 每任务模型、推理强度、权限和 Goal 状态管理；
- Markdown、数学公式、表格、Diff、图片及常见服务器文件预览；
- 服务器目录选择、文件管理、上传和下载；
- 90 天可信设备、设备管理、断网重连和离线外壳；
- 分用户 systemd 安装、诊断、原子 ZIP 更新和兼容卸载。

### 本版修复

- 保留任务实际记录的模型，即使该模型尚未出现在 app-server 模型目录中；
- `gpt-6-astra` 等目录外模型不再被错误显示成默认模型；
- 兼容任务设置位于 `thread/read`、`thread/resume` 顶层或嵌套字段的响应；
- 归档任务或实时订阅暂时失败时仍显示其已记录模型和推理强度；
- 目录外模型的推理强度继续使用统一的中英双语标签。

### 发布验证

- Node.js 语法检查通过；
- 自动化测试 62/62 通过；
- `npm audit --omit=dev` 报告 0 个已知漏洞；
- 干净发布包不包含旧 Git 历史、运行凭据、Codex 会话、依赖目录或个人工作文件。
