# Changelog

本项目采用 `v主版本.次版本.修订版本` 标签。首次对外分发从 `v0.18.7` 开始；更早版本属于内部开发历史，不应上传或分发。

## v0.19.6 - 2026-09-18

- 调整侧栏底部第三行布局：连接状态使用中间和右侧的连续空间，状态文字居中，圆点不再把文字推向右侧；蒲公英私网地址允许完整换行显示。
- 通知按钮在 HTTPS 不可用时将提示拆成两行，避免“通知（需 HTTPS）”被截断；移动 Chrome 真实布局回归通过。Service Worker 更新为 v121。

## v0.19.5 - 2026-09-18

- 精简侧栏：合并品牌标题、缩小 Logo、收窄默认宽度，并把新建任务与搜索任务并列；移除会话来源/状态辅助行和本机标签入口，任务卡片改用最新消息预览。
- 侧栏底部改为紧凑 3×3 功能区，把通知、连接状态和私网地址放入同一视区；移动端页面保持固定，只有任务列表承担滚动。
- 新增搜索任务弹窗，支持标题和最近几条用户/助手消息匹配、摘要显示、归档任务去重、授权目录过滤和防抖取消旧请求；清除按钮只有一个，避免重复叉号。
- 消息时间行移到正文/气泡下方并保留无障碍时间说明；Service Worker 更新为 v120。
- 新增搜索、最近消息预览、授权目录和归档去重的后端回归，以及移动 Chrome 搜索弹窗回归。

## v0.19.4 - 2026-09-17

- 修复实时事件连接已断开时，晚到的普通 HTTP 轮询将连接提示恢复为“服务器已连接”的问题。事件流独立记录连接状态；连接尚未恢复时，HTTP 状态回复保留断线提示。重叠轮询与重连读取的浏览器回归固定覆盖此场景，Service Worker 为 v119。
- 发布工作流先等待 Node 22/24 检查全部通过，再执行完整打包并创建 Release；分支检查和标签发布共用同一检查工作流。
- 包含 v0.19.3 的复选框标签触摸尺寸修复。

## v0.19.3 - 2026-09-17

- 为移动端复选框的关联标签补齐 44px 最小宽度，避免缺少中文字体或文本较窄时触摸区域缩至约 40.84px；最小高度继续保持 44px。Service Worker 更新为 v118。
- Chrome 153 的受限字体环境已先复现尺寸失败，修复后完整移动浏览器回归通过；系统字体、真实手机字号、触摸和读屏验收继续按既有范围跟踪。

## v0.19.2 - 2026-09-17

- 正式交付 0.19 系列的消息时间、任务恢复、写入与审批保护、阅读和移动交互改进；能力边界及仍需实机验收的范围见 README 和相关验收文档。
- 修复静默同步当前任务时意外关闭文件菜单、丢失键盘焦点的问题；主动切换任务仍关闭旧菜单。与延迟滚动事件的菜单回归一起固定验证，Service Worker 更新至 v117。
- 云端持续检查同时覆盖 Node 22 和 Node 24，失败记录包含菜单关闭调用链。此前检查失败的 v0.19.0、v0.19.1 标签保留，未替换已推送标签。

## v0.19.1 - 2026-09-17（标签保留，未生成正式 Release）

- 准备下述 0.19 系列改进的分发；保留已推送但发布检查失败的 v0.19.0 标签，使用新修订版本完成正式分发。
- 修复列表刷新后延迟滚动事件关闭刚打开菜单的竞态：触发按钮位置未变化时保留菜单和键盘焦点；实际滚动使按钮移动时仍关闭菜单。浏览器回归固定覆盖这两条路径，Service Worker 更新至 v116。
- CI 将失败断言直接写入 GitHub 检查摘要；浏览器失败包含浏览器版本与焦点现场，便于只有仓库专用 Deploy Key 的维护者排查。

## v0.19.0 - 2026-09-17（标签保留，未生成正式 Release）

### 验收

- CLI 0.153.2 的持续真实模型恢复补验 8 组通过：245 秒的实际生成经历桥接断线、PWA SIGKILL 和离线完成后找回完整结果；原生 daemon SIGKILL 后正确显示已停止，重新打开不自动重跑，明确续写才新增完成轮次。使用同账号只读认证挂载和独立状态，生产服务与历史不变；失败、超时、模型流重试及适用范围见 `docs/TASK_RECOVERY.md`。
- 新增 `npm run release:candidate`，在完整测试、隐私扫描、清单和暂存包运行验证后保留可审阅的本地候选 ZIP、来源/内容摘要及外部校验文件。支持未提交工作树，不创建提交、标签或正式 Release；正式打包的干净工作树和标签要求保留。
- CLI 0.153.2 独立账号的原生审批恢复通过：Web UI SIGKILL 后重新打开任务可重新取得待审批请求，旧确认令牌被拒绝，新令牌才能执行测试命令；独立原生 daemon 在待审批时 SIGKILL 后，历史显示已停止，旧审批和未执行命令不会重放。该审批验收的模型响应由本机固定夹具提供，生产 daemon 不变；另行完成的持续真实模型验收及边界见 `docs/TASK_RECOVERY.md`。
- 独立临时 Linux 账号下，真实 CLI 0.153.2、完整安装器、用户 systemd 和私网 socket proxy 的 5 组验收通过；重复安装保留密码和设备登录，操作系统拒绝访问原账号 daemon，生产服务不变。使用已有独立 daemon 路径与合成登录占位值，未验证真实模型认证或手机访问。另已核对公开 v0.18.15 下载资产、清洗 Git bundle 与远端基线，详情见 `docs/RELEASE_ACCEPTANCE.md`；它们不代表新版本已发布。
- 新增 `npm run verify:live-recovery -- DAEMON_SOCKET TASK_ID`，对一个真实运行任务验收独立 Web UI 的冷启动、桥接断线、SIGKILL 重启、登录保留及恢复事件回放。转发层只允许指定任务的无正文元数据读取，不转发订阅、写入或审批；不重启生产 PWA 或共享 daemon。真实 0.153.2 验收 5 组通过，仍不代替 daemon 重启、真实审批及手机交接验收，详见 `docs/TASK_RECOVERY.md`。
- 新增显式 `npm run verify:message-identity -- CLI [CLI ...]`：使用真实 CLI、临时状态目录、仅内存任务与本地无生成端点，核对消息关联字段的实际回传；不连接生产 daemon、不调用外部模型。字段用途和设备识别边界见 `docs/CLI_COMPATIBILITY.md`。
- 新增显式 `npm run verify:cli -- OLD_CLI NEW_CLI`，用实际 CLI 生成实验协议并通过完整 PWA 启动，核对版本、模型读取和临时状态库的版本切换。0.148.0 与 0.153.2 均通过，公开快照不包含任务或凭据；矩阵和边界见 `docs/CLI_COMPATIBILITY.md`。
- 发布验证和本地打包增加暂存包实际运行检查：从完整候选包离线安装依赖，生成隔离配置并启动真实服务，核对登录/CSRF、授权目录、全部前端资源及缓存列表、PDF 依赖和审批身份。使用模拟 daemon，保持生产服务运行；缺失缓存资源的故障回归确认静态检查通过时仍会拒绝坏包。需提前准备 npm 依赖缓存，边界见 `docs/RELEASE_ACCEPTANCE.md`。
- 新增显式 `npm run verify:systemd`：独立安装完整应用，使用真实安装器生成的临时用户 unit，核对登录/CSRF、文件授权、静态资源、重启持久化和异常退出自动恢复。已通过 8 组验收，保留现有 PWA、私网代理和共享 daemon；测试不会创建或写入任务，范围与剩余发布验收见 `docs/RELEASE_ACCEPTANCE.md`。

### 修复

- 根据 CLI 0.153.2 原生多连接/跨进程实验修正写入冲突提示与切换说明：冲突不证明另一台设备正在生成，退订或关闭连接也不保证释放进程占用。同一 daemon 可接受另一连接向当前轮次追加输入；PWA 队列范围保持为本服务，不显示未经确认的外部设备身份或接管成功。Service Worker 更新至 v115。
- 修复重复默认安装时再次执行 `daemon bootstrap`、替换已运行 Codex daemon 的问题。安装器先查询并复用运行实例，仅在查询未发现运行实例时初始化；成功返回但格式无法识别时停止，不操作 daemon。独立账号实际复现与安装回归覆盖此路径。
- 通知设置显示权限、浏览器注册和服务器保存进度；原生注册超过 10 秒时说明可关闭设置窗口并保持页面打开。等待期间阻止重复注册，服务器确认后才显示已开启；成功/失败清理延迟提示，状态播报不再被整个弹窗的忙碌标记延后。Service Worker 更新至 v114。
- 后台推送的私有 VAPID 配置增加可选 HTTP/HTTPS 出站代理，适用于服务器无法直连厂商服务的部署。默认直连，不自动读取通用代理环境变量；设备订阅接口不能选择代理，凭据不进入前端状态或订阅文件。实际 CONNECT 传输、错误记录及重复配置保留已有回归，范围见 `docs/WEB_PUSH.md`。
- 修复不同系统字体在 200% 字号下使弹窗关闭按钮超出固定高度的问题；图标按钮保留最小触摸尺寸，高度随内容伸展。独立新账号的实际更新检查先复现，移动 Chrome 回归补充较高行盒，Service Worker 为 v113。
- 修复从其他目录通过绝对路径调用安装器时，`npm ci` 错用调用目录的问题；依赖始终安装到 PWA 程序目录。Codex daemon bootstrap 失败时说明 managed standalone 安装前提和已有 daemon 的复用方式，并在修改 PWA 服务配置前退出；两项均有故障复现与回归。
- 新建任务、发送和追加消息携带独立消息关联标识，页面按同一任务内的协议回声合并发送气泡；另一设备发送相同正文或缺少标识的消息不会确认本机待发送内容，也不会继承其本地时间。标识不作为设备身份或重试幂等凭据；Service Worker 为 v112。
- 生成图片即使命中实时缓存，也在 GET/HEAD 返回前重新检查当前任务目录授权；恢复归档任务在写入 Codex 前检查授权，并规范化检查返回目录。来源元数据文件改用 Promise 文件 API，修复实际 rollout 读取失败。
- 命令审批遵守上游 `availableDecisions` 的允许范围和顺序，前后端共同拒绝未提供的决定；显式空列表不回退为全部批准，尚不支持的规则修改选项会说明处理入口。命令的额外权限、网络目标和理由保留在摘要及确认中，Service Worker 为 v109。
- 能力诊断不再用子串匹配推断支持，明确布尔声明优先于名称推测；真实 CLI 未声明独立协议版本或能力时保持未知。现代与旧式审批响应映射集中到协议适配层。
- 新版文件审批按任务、轮次和条目标识关联实际文件变更通知，摘要及二次确认显示文件影响、删除风险和移动目标；没有匹配明细时明确提示待核实。上下文有界保存，不保留 Diff 正文，不把历史条目猜作当前审批。
- 文件明细晚到或 patch 内容更新会使旧确认及排队中的旧决定失效；相同内容回放保持当前请求，断线后不复用旧上下文。新增协议/隔离 HTTP/移动 Chrome 回归，Service Worker 为 v108，范围见 `docs/PROTOCOL_AUDIT.md`。
- 修复连续打开确认框时，前一次延迟到达的原生关闭事件意外关闭新确认框的问题；文件摘要被截短时，确认内容仍保留已知删除风险。
- 旧式命令/文件审批现在按协议的 `conversationId` 关联任务，经过相同的目录授权和写入队列；修复旧式请求缺少 `threadId` 时绕过这两步的问题。交互请求缺少其协议声明的任务标识时明确拒绝，不生成可操作审批卡。
- 文件审批识别旧式路径字典、嵌套删除类型和移动目标；有说明文字时仍保留具体文件变更，摘要及二次确认显示请求的写入范围。移动 Chrome 回归覆盖风险提示与取消确认，Service Worker 为 v107。当前协议证据与尚未覆盖的 v2 条目关联见 `docs/PROTOCOL_AUDIT.md`。
- 审批和问答绑定每次请求的独立标识；旧确认框、旧排队操作和延迟成功响应不能作用于复用同一 RPC 编号的新请求。新版后端拒绝缺失/过期标识并提示刷新，发送给 Codex 前再次核对请求实例。
- 审批/问答确认及提交期间禁用重复操作，未改变的请求在快照和回放中保留正在填写的内容及提交状态；失败后恢复控件和原回答。推送去重与待处理检查也区分同编号的不同请求。Service Worker 为 v106，协议与升级边界见 `docs/PROTOCOL_AUDIT.md`。
- 修复断线重连后任务状态读取失败、页面隐藏或同步尚未完成时仍提示“已同步”的问题；失败会明确提示并自动退避重试，只有状态核对成功才确认恢复。断线/退出后旧响应和延迟提示失效，重放缺口在同步期间到达会追加一次最新读取。Service Worker 为 v105。
- 修复页面恢复连接时把异常、未加载或未知任务笼统报成“已完成”的问题；分别报告运行、等待、空闲、异常和待核实，搜索/分页遗漏的活动记录继续有界保留。列表不再将空闲等同于成功，并可分别筛选未加载和未知状态。
- 核心状态用语集中到共享模块，任务列表、活动记录、页面通知和 Web Push 使用一致的状态文案；修复规范 `systemError` 状态遗漏提醒、未知结束状态被通知为成功，以及失败轮次缺少错误对象时没有失败提示的问题。Service Worker 为 v104，验收范围见 `docs/TASK_RECOVERY.md`。
- 手机主要按钮、筛选和菜单采用至少 44 CSS 像素的触摸区域；任务和文件复选框保留紧凑图形，使用关联标签扩大命中范围。修复文件夹内复选框的空格键和标签点击触发目录导航的问题。
- 界面字号改用相对单位，默认字号下保持原有文字尺寸；短屏幕上的侧栏与弹窗可滚动，输入区选项可换行。历史节点虚拟行高随实际字体尺寸重新测量，字体放大不再裁切日期或使滚动范围失真。Chrome 验证 200% 基准字号、320×480 CSS 像素视口和触摸事件，Service Worker 更新至 v103；实机范围见 `docs/ACCESSIBILITY_AUDIT.md`。
- 完整历史窗口按轮次复用重叠内容，移出窗口时仅保存轻量展开状态，返回后恢复历史折叠组和长回复展开；上下文追加保留当前焦点和阅读行位置，位置补偿不再受平滑滚动影响。历史定位反馈在滚动后仍显示于上下文提示下方。
- 相同活动快照保留命令/工具输出展开、已加载 Diff 和失败轮次重试按钮；完整记录替换只移除消失的条目，并保留单独加载的图片。修复运行记录按条数/文字长度比较而遗漏等长修正、命令输出和工具结果变化的问题；比较缓存限制为 8 条、每条最多 65,536 字符，超限记录继续渲染。Service Worker 更新至 v102。
- 修复任务列表刷新/排序时丢失勾选框焦点与阅读位置的问题，按任务 ID 复用卡片和项目分组；显式同步期间保留已有列表，任务移除后将焦点移至相邻控件。内容未变化的消息同步保留章节目录展开、正文选择范围和链接焦点，避免反复重建 Markdown。
- 历史节点虚拟列表复用按钮，补充方向键、Home/End、PageUp/PageDown 与列表位置语义；滚动出屏幕的焦点最多额外保留一行，筛选不抢走搜索焦点。修复数据变少后滚动范围越界，以及后台对话滚动关闭文件弹窗菜单的问题。新增 Chrome 行为回归，Service Worker 更新至 v101；完整历史窗口换页与实机读屏等剩余范围见 `docs/ACCESSIBILITY_AUDIT.md`。
- 修复手机侧栏/详情面板关闭后仍可读或聚焦、弹窗返回隐藏按钮、原生弹窗内菜单位于模态层外的问题；补充菜单键盘导航、嵌套弹窗顺序、表单名称及当前界面内的简短状态朗读。新审批到达或旧审批移除时，保留正在填写的回答、焦点和选择范围。提高危险确认按钮和辅助文字对比度。Chrome 实际语义树核对覆盖 17 个弹窗，主题对比度覆盖 64 组令牌配色，Service Worker 更新至 v100；实机读屏与其他剩余范围见 `docs/ACCESSIBILITY_AUDIT.md`。
- 增加已知运行任务的私有状态记录与只读恢复：服务重启后先标为待核实，重连和后台轮询核对状态，无浏览器连接时也可发现遗漏的完成；不自动订阅或取得写入权。旧轮次结果不再清除新轮次，恢复完成与实时完成共用推送去重。新增隔离进程崩溃/重连、权限移除和移动 Chrome 回归，Service Worker 更新至 v99，范围见 `docs/TASK_RECOVERY.md`。
- 增加可选 Web Push：可信设备独立订阅/关闭，PWA 在无页面事件连接时可投递任务状态；发送前检查设备会话、目录授权和待审批状态，退出或撤销后停止新投递。使用 `web-push` 的标准加密和 VAPID 签名，不发送任务正文、命令、路径或原始错误信息。
- 新增通知设置面板、Service Worker push 接收与去重、一次性私有密钥生成和配置诊断；重复安装保留推送、动态授权和回放文件位置。新增 HTTP、加密/签名和 Chrome 离开页面后注入 push 的回归。Service Worker 更新至 v98；真实厂商服务与手机端到端送达仍待验收，生产未配置启用。见 `docs/WEB_PUSH.md`。
- 修复当前任务在页面隐藏后完成、失败或等待审批时漏发通知的问题；后台当前任务也会保留未读标记，返回对话后清除。中止任务的提醒不再显示为已完成。
- 通知优先使用 Service Worker 的 `showNotification`，兼容不支持页面通知构造器的手机浏览器；点击通知可打开对应任务并保留已有页面草稿。失败投递不占用去重窗口，内存及持久去重记录均限制为 256 条并清理过期/未来时间。Service Worker 更新至 v97。
- 通知入口明确标示页面运行时的提醒范围，区分非 HTTPS 和浏览器缺少通知 API；尚未提供关闭页面后的新消息推送。新增实际 Chrome 通知 API、受控后台状态、通知跳转和草稿保留回归。
- Git 更新改为完整独立候选目录中的 fast-forward、依赖安装和检查，通过后与 ZIP 更新共用切换、健康核对和自动回滚。候选失败不再提前改动原安装的 HEAD 和文件；保留 Git 配置、相对本地 remote 和被忽略的本机文件，最新提交不重复重启。
- Git 更新拒绝替换有关联 worktree、脏工作树或分叉历史的安装，并在切换前复查原 HEAD 和 Git 本地改动。备份清理按安装路径隔离，避免删除同目录下其他安装或旧命名备份。
- ZIP 更新在执行 npm 前校验完整发布清单；核对健康接口的 `ok`、运行版本及 Service Worker 全文摘要，避免旧缓存或不健康响应被误判为成功。回滚后再次验证旧服务，恢复未确认时明确报错并保留失败候选。
- 更新增加安装目录互斥锁和唯一备份目录，避免并发更新或同秒连续更新破坏旧版本备份。隔离流程验证保留配置、密码及可信设备。
- 修复安装脚本调用 Node/Codex 时遗漏路径引号，支持可执行文件和安装目录含空格；新增实际脚本、npm 安装与独立 HTTP 服务的安装/重复安装/更新/回滚集成验收。
- 根据 CLI 0.153.2 生成协议接入通知信封的 `emittedAtMs`：延迟投递和 SSE 回放使用原事件发出时间，并标注为近似消息时间；精确消息字段优先，旧版缺失字段继续回退。Service Worker 更新至 v96。
- 修复其他客户端解决审批/问答后，后端重连状态仍保留旧请求、排队回答可能继续发送的问题；前端同步清理后台任务请求，并阻止旧状态快照将其恢复。新增 HTTP/WebSocket 队列竞争及移动 Chrome 回归。协议证据和未验收范围见 `docs/PROTOCOL_AUDIT.md`。
- 将对话消息视图渲染、乐观消息和流式回复更新移入 `public/message-view.js`，主应用继续注入时间、Markdown、复制和滚动依赖；Service Worker 更新到 v91。
- 将工作目录选择器的目录根、面包屑、搜索、创建目录和目标目录选择流程移入 `public/directory-browser.js`，主应用继续注入授权范围、请求和确认依赖；Service Worker 更新到 v92。
- 将服务器文件浏览器的搜索、预览、下载、批量操作、上传进度/暂停和目录刷新流程移入 `public/file-browser.js`，主应用继续注入授权校验、菜单、确认和上传限制依赖；Service Worker 更新到 v93。
- 将命令输出、Diff、文件变更、工具调用、推理、计划、上下文提示和图片工件视图移入 `public/activity-view.js`，主应用继续注入任务 API、滚动、时间和状态依赖；Service Worker 更新到 v94。
- 将任务列表视图的筛选、项目/标签选项、批量选择、长按菜单、分组卡片和时间显示移入 `public/thread-list-view.js`，主应用继续负责数据加载、操作回调和持久化状态；Service Worker 更新到 v95。
- 协议适配层现在在 bridge 路由前归一化旧版 camelCase 通知，并保留有界 `originalMethod` 诊断字段，使状态更新、写入占用和 SSE 广播共享同一规范方法名。
- 浏览器后台通知的去重时间戳改为有界本地持久化，刷新或短暂重连不会在短时间内重复提醒同一任务；只保存任务 ID 和时间。
- 消息时间支持带时区的 ISO 字符串、秒和毫秒字段；无效日期不再触发格式化异常。优先读取消息对象和通知中的发送/开始/完成字段，轮次缺失时不凭空生成历史时间，也不把运行中轮次的更新时间当成完成时间。
- 乐观消息合并时转移时间元数据并保留本地估计标记；流式渲染保留开始和完成时间及来源。历史仅有轮次时间时明确标为回退，悬停可查看时间依据。
- 修复任务内容已显示但地址栏仍等待图片列表完成才更新的问题，避免立即刷新后丢失当前任务；移动 Chrome 回归用延迟图片响应验证刷新恢复与时间一致性。
- 网络断开或写入响应 JSON 不完整时，前端现在将结果视为尚未确认，避免把已送达的消息当作失败而提示重复发送；可通过有界的服务器结果凭据恢复成功或取消结果。
- 修复发送期间切换任务后，旧请求结果污染当前任务状态或草稿的问题；取消等待会恢复原输入并保留期间新写的草稿，草稿延迟保存也绑定到原任务和原输入。
- 修复写入结果暂时未知的乐观消息在服务器正式回声或历史刷新后仍保留“结果未知”虚线和提示的问题；正式消息确认时会清理该状态，并保留本地时间元数据直到服务器提供权威时间。
- 修复 SSE 静默恢复请求晚于用户打开历史节点时覆盖历史上下文视图的竞态；恢复响应会尊重当前正在查看的历史上下文。
- 任务信息面板新增写入证据摘要，分别显示 Codex 报告的直接写入能力和 PWA 自身的实时订阅状态；不再用任务创建来源推断当前由 Windows、CLI 或其他客户端写入。
- app-server 返回活动写入冲突后，服务器会在任务详情中保留 10 分钟的有界观察证据（来源、时间和冲突轮次）；界面显示为最近观察到的冲突，不伪造外部客户端名称。HTTP 回归覆盖冲突后的详情核对。
- 新安装的 `install-user.sh` 默认只授权 PWA checkout；已有安装会继续读取现有 `CODEX_PWA_ROOTS`，必须通过 `--root` 显式加入项目目录。示例配置同步改为显式项目路径，并新增新安装默认范围回归。
- 未知 app-server 通知诊断现在在任务信息面板显示有限数量的最近方法、次数和最后观察时间，保留协议升级线索而不保存通知正文。
- 审批风险识别补充结构化文件变更的删除、覆盖、替换和截断动作，并识别 shell 重定向；高风险审批继续使用现有二次确认流程。
- 协议适配层新增浏览器可缓存的通知方法别名归一化，兼容旧版 camelCase 事件名称后再进入统一处理流程；Service Worker 更新到 v73，并保留未知事件诊断。
- 将审批详情、命令/路径提取和风险策略移入 `public/approval-policy.js`，新增结构化破坏性变更单元测试；Service Worker 更新到 v74。
- 文件浏览器新增有界的授权范围搜索；默认仍筛选当前目录，启用“搜索授权范围”后会递归搜索授权根目录并保留所属根目录和相对路径上下文。搜索不跟随符号链接，默认继续隐藏敏感条目，结果最多 200 项且受目录访问上限约束；达到上限时显示搜索专用提示，目录结果没有可用修改时间时不再显示空字段；Service Worker 更新到 v75。
- SSE 连接新增显式 heartbeat 数据帧；前端连续约 60 秒未收到 heartbeat 时主动关闭假连接并进入回放重连，页面长时间断网后更容易恢复；Service Worker 更新到 v76。
- 文件浏览器新增“管理授权目录”流程；基础 `CODEX_PWA_ROOTS` 目录保持不可移除，用户可在明确二次确认后追加或移除最多 32 个额外目录。变更受登录、CSRF 和专用请求标记保护，保存于权限为 600 的授权记录文件，并实时更新文件 API 范围；Service Worker 更新到 v77。
- 任务信息面板现在区分基础授权范围和 Web UI 追加的额外目录，减少用户误以为所有路径都来自初始配置的情况；Service Worker 更新到 v78。
- 将授权目录管理的渲染、加载、追加和移除流程移入 `public/access-roots.js`，主应用继续通过依赖注入连接确认、状态刷新和 SSE 更新；Service Worker 更新到 v79。
- 将置顶、重命名、批量已读、归档和恢复任务的操作流程移入 `public/thread-actions.js`，保持任务列表、对话菜单和确认行为不变；Service Worker 更新到 v80。
- 将线程操作模块继续收口，纳入重命名对话框和长按操作菜单的目标状态管理；Service Worker 更新到 v81。
- 将审批决定和问答提交流程移入 `public/approval-actions.js`，保留二次确认、写入队列、冲突刷新和失败提示；Service Worker 更新到 v82。
- 对话顶部将任务的历史创建来源明确标为“创建来源”，避免被误解为当前 Windows、CLI 或其他客户端的实时写入者；Service Worker 更新到 v83。
- app-server 写入冲突若带有结构化 `activeWriter`/`writer` 字段，现在会以有界的“协议报告写入者”证据保留并显示；缺少字段时继续明确显示未知，不根据任务创建来源推断；Service Worker 更新到 v84。
- 修复同一任务写入队列在处理失败后留下未捕获 Promise 拒绝、导致 Web UI 进程退出的问题；新增独立进程和 HTTP 写入冲突恢复回归。
- 对发送、追加、中止、审批和问答的排队请求增加断线取消、30 秒等待上限、每任务 32 条待处理和全局 1,024 条操作上限。超限/等待超时明确返回“尚未发送”；已开始执行的操作仍保持串行，客户端断开不会提前释放队列。
- 补齐发布清单遗漏的 `protocol-adapter.mjs` 和 `event-replay.mjs`；发布验证现在解析暂存包内 JavaScript 的静态相对导入和重导出，检查依赖是否遗漏或越出发布目录，并验证实际发布清单。

### 改进

- 设置、文件/预览、设备、目录授权、Goal、审批、任务操作、历史节点、诊断与弹窗文案统一到 `public/ui-copy.js`。操作名称统一为“任务”“停止”“退出”，确认框小标题使用中文；保留专有名称与审批的会话范围语义。静态页面同步检查同时覆盖可访问名称，校验提示中文化不改变协议枚举、HTTP 状态码或未知结果标记，Service Worker 为 v111。维护方式见 `docs/UI_COPY.md`。
- 完成任务、诊断及协议模块边界拆分：任务路由、数据读取、订阅生命周期和图片缓存各自管理；SSE 打开/回放归入事件模块，协议降级与冲突字段归入适配层。前端新建/发送/中止、通知处理及诊断视图各有独立模块，保留原确认、草稿和恢复行为。新增实例隔离及授权时机回归，Service Worker 为 v110；职责与证据见 `docs/PROTOCOL_AUDIT.md`。
- 协议适配层现在兼容初始化响应中的嵌套 `serverInfo/server` 能力、`advertisedCapabilities`、`supports*` 字段和方法列表，并继续输出固定能力矩阵；新增 CLI 形状回归夹具，减少升级后能力诊断误报。
- app-server 通知新增桥接服务首次接收时间，SSE 缓存与回放保留原值；浏览器接收时间、服务器接收时间和消息实际记录时间分别标注。时间解析与优先级集中到 `public/message-time.js`，当前任务的时间缓存限制为 4,096 项，Service Worker 更新到 v72。
- 发送、追加、中止、审批和问答新增请求编号、按登录设备校验的状态查询/取消接口，以及输入框上方的等待状态与“取消等待”。仅服务器确认仍在排队的操作能取消；已开始的操作继续核对结果。提示可显示同一 PWA 服务实际识别到的等待设备，不用任务创建来源推断外部客户端的写入者。
- 写入结果凭据仅在内存中保留最多 128 条、有效期 2 分钟，每条响应正文最多 32 KiB；过期或缺失保持结果未知。新增客户端、设备隔离/CSRF HTTP 和移动 Chrome 回归，Service Worker 更新至 v71。
- 将每任务模型、推理强度和权限设置的状态、目录同步及选项控件移入 `public/task-settings.js`，保留生效值与下一轮待应用值的边界，并将 Service Worker 更新到 v70。
- 将可信设备列表、设备重命名/撤销、其他设备注销和凭据修改移入 `public/device-manager.js`，保留认证 API 与确认流程，并将 Service Worker 更新到 v69。
- 将 Markdown、LaTeX、代码块、表格和服务器文件链接渲染移入 `public/markdown-renderer.js`，保留消息目录与安全清洗行为，并将 Service Worker 更新到 v68。
- 将任务收件箱的最近时间、项目/标签/未读/状态筛选策略移入 `public/thread-list.js`，新增独立策略测试，并将 Service Worker 更新到 v67。
- 为文件上传链路增加真实 multipart 故障回归：注入 `ENOSPC` 或 `EDQUOT` 时统一返回 HTTP 507 和安全中文提示，临时文件与目标文件都会清理；当前完整检查为 100/100。
- 增加 legacy 与当前 Codex initialize 响应夹具，验证 CLI/app-server 升级后版本字段和能力矩阵仍能稳定归一化。
- 新增 `npm run release:verify` 开发树发布验证闸门：检查完整测试、发布清单、符号链接和敏感信息扫描，不要求提交/标签，也不生成或发布正式归档。
- `.env.example`、README 和 `npm run doctor` 现在记录并检查 SSE 回放文件位置、大小和 600 权限，便于排查重启恢复与本地权限问题。
- 调整浅色主题状态色和深色主题主按钮色，使 green/blue/amber/red/purple 状态提示及普通/悬停按钮文字达到 WCAG AA；回归测试覆盖全部主题语义色，并将 Service Worker 更新到 v66。
- 信息面板新增 Codex app-server 宣告能力摘要，区分 PWA 桥接能力与当前 CLI 实际支持的实验接口；Service Worker 更新到 v65。
- `/api/status` 和任务详情信息面板现在显示有界 SSE 事件回放的持久化状态、事件数量和窗口信息，便于诊断重连恢复能力；新增回放元数据测试。
- 将统一 API 请求、CSRF 注入、401 重新登录处理和网络错误状态移入 `public/api-client.js`，新增独立回归测试，并将 Service Worker 更新到 v64。
- 提升深色和浅色主题的次要文字颜色，使其在主要面板和侧栏背景上的对比度达到 WCAG AA；新增双主题对比度回归测试，并将 Service Worker 更新到 v63。
- SSE 回放缓存在生产 Web UI 重启后会从用户 Codex 配置目录恢复，仍受事件数量和 8 MiB 大小上限约束；新增持久化恢复回归，短暂重启不再立即丢失事件窗口。
- 将 SSE 客户端队列、背压处理、广播、设备流清理和断线关闭移入独立的 `sse-events.mjs`，服务端通过依赖注入保留事件回放和认证设备流行为，并加入发布清单。
- 对原生对话框统一补充键盘焦点初始定位、关闭后的焦点恢复和可见焦点轮廓，改善手机浏览器与键盘/屏幕阅读器用户在确认、文件、设备和设置对话框中的连续操作，并同步更新 Service Worker 到 v62。
- 将认证 API、可信设备、CSRF、登录限流和登出流移入独立的 `auth-api.mjs`；服务端主文件通过依赖注入保留现有认证行为，并把该模块加入发布清单。
- 将目录浏览、文件列表、上传、预览以及批量文件操作移入独立的 `file-api.mjs`；保留真实路径、符号链接、授权根目录和跨文件系统校验，并把模块加入发布清单。
- 将 Codex app-server 的 isolated/shared-daemon 传输、协议初始化、RPC 超时与重试、审批请求、通知桥接和断线重连移入独立的 `app-server-bridge.mjs`；通过依赖注入保持共享 daemon、任务所有权和图片工件行为，并把模块加入发布清单。
- 将静态资源路径校验、内容类型和缓存策略移入独立的 `static-server.mjs`，服务端主文件只负责注入目录和错误响应；发布清单和静态资源回归同步更新。
- 中英文混排的界面 eyebrow 标签补充 `lang="en"` 语义；动态确认操作会根据实际文案切换中英文语言标记，改善屏幕阅读器发音一致性，并同步更新 Service Worker 到 v60。
- 安装器在默认授权根目录覆盖整个用户 home 时明确输出安全警告和 `--root /absolute/project/path` 收紧建议；显式指定根目录的现有安装行为保持不变。

- ZIP 更新器在重启候选版本后同时核对健康接口中的 Web UI 版本和实际 `sw.js` 响应，只有运行版本与候选包一致且 Service Worker 可用时才确认更新成功，否则沿用原子回滚流程。
- 增加真实共享 daemon 的双客户端并发写入回归：两个 HTTP 客户端同时向同一任务发送消息时，桥接层会在到达 app-server 前串行处理，并验证两次请求都能安全完成。
- 将 SSE 会话游标读取、持久化和有界事件去重移入独立的 `public/event-session.js`，保留刷新恢复、回放去重和 1,024 条上限，并同步更新 Service Worker 到 v59。
- 主要对话框补充 `aria-labelledby`/`aria-describedby` 关联，让屏幕阅读器打开原生对话框时能直接获得标题和必要说明；消息重试/继续、回复和代码复制、批量管理及文件/Goal/设备操作按钮的最小触摸高度统一提升到 36px，并同步更新 Service Worker 到 v59。
- 主要用户消息和 Codex 回复开始显示具体时间；实时消息优先使用 app-server 通知时间，旧历史使用消息或轮次时间回退，乐观消息会标记为本地记录时间。
- 跨客户端发送、追加和中止任务时，将写入冲突规范为 409 `THREAD_WRITE_CONFLICT`，并让 Web UI 刷新任务状态、保留输入内容和提示用户等待其他客户端完成操作。
- 任务列表新增持久化未读标记；后台任务完成、开始或等待审批时提示对应任务有更新，打开任务后自动清除。
- SSE 广播增加有界事件 ID 回放和前端去重；短暂断线重连时可接续遗漏事件，超出回放窗口则自动执行完整状态恢复。
- 文件浏览器默认继续隐藏敏感目录和凭据/密钥文件；用户显式打开“显示隐藏文件”后，列表会标注“敏感目录/敏感文件”，现有授权根目录和直接路径校验保持不变。
- `/api/status` 现在记录 app-server 初始化返回的 CLI/App Server/协议版本和桥接能力矩阵；前端信息面板显示这些诊断信息，并统计未知通知，便于识别 Codex CLI 升级后的协议差异。
- 协议快照归一化已移入独立的 `protocol-adapter.mjs`，并增加不同初始化返回结构的回归覆盖，降低后续协议兼容修改对主服务文件的影响。
- SSE 事件 ID、回放窗口裁剪和回放缺口检测已移入独立的 `event-replay.mjs`，并增加边界测试，降低断线恢复逻辑的维护风险。
- 任务列表新增收件箱状态筛选，可快速查看未读、运行中、等待操作、异常和已完成/空闲任务；原有最近、全部历史和归档范围继续独立生效。
- 断线重连完成后显示恢复结果和接收的实时更新数量；若事件已超出回放窗口，会明确提示已重新同步任务状态。
- 审批卡增加风险级别、工作目录、命令/文件影响、模型和权限模式摘要；原始参数仍保留，危险命令会使用更醒目的风险标识。
- `npm run doctor` 增加源码提交、分支、工作树状态、配置格式、服务工作目录和 MainPID 诊断，能区分当前开发树与可发布快照。
- 在 HTTPS 或本机安全上下文中增加显式浏览器通知开关；后台任务完成、失败和审批/问答请求会提醒其他任务，单任务通知带 30 秒限流，私网 HTTP 页面会明确提示需要 HTTPS。
- 发送或创建任务遇到可重试失败时，输入内容会保留并在提示中提供“重试”；跨客户端写入冲突会先刷新任务状态，结果未知的请求仍不会自动重复提交。
- Web UI 会保存有限的后台活动快照；重新打开或重启后会根据当前任务列表提示仍在运行或已经完成的后台任务，帮助用户判断恢复结果。
- 服务器文件浏览器支持多选项目并批量删除；批量接口限制为最多 100 个路径，只允许删除并逐项报告失败，继续使用单项目路径、符号链接和非空目录安全校验。
- 文件浏览器多选还支持批量移动和复制；移动/复制共享目标目录、同名不覆盖和跨文件系统清理规则，并逐项返回成功或失败结果。
- 服务器文件上传显示实时进度和已上传字节数，并可通过同一按钮取消当前 XHR；取消会清理状态并保留现有单文件/批次大小限制。
- 服务器文件浏览器上传现在支持暂停和继续；暂停会保留待上传文件，继续时明确从头上传，避免把一次性 multipart 请求误报为可断点续传。
- 切换对话时不再误中止服务器文件浏览器的上传请求；对话附件上传仍会随任务切换安全取消。
- 对话轮次和 Codex 回复增加 `aria-busy`、失败/中止状态语义和角色标识；详情面板标签补充选中状态与关联面板，隐藏项开关文案改为“显示隐藏目录/文件”。
- `npm run doctor` 在有远端仓库时以限时只读方式报告远端 `main`、远端最新 Release 标签及本地提交/标签差异；网络或权限不可用时保留诊断结果并明确标为未知。
- 增加真实 app-server 断线回归：daemon socket 不可用时，Web UI 页面和静态 HTTP 面仍保持可访问，健康接口连续报告桥接未就绪，服务进程不退出。
- 将后台任务快照的存储解析、活动快照构建和重启对账移入独立的 `public/task-snapshot.js`，并加入边界单元测试；PWA Service Worker 同步缓存该模块。
- 将浏览器通知能力检测、权限请求、点击跳转和限流移入独立的 `public/browser-notifications.js`，并加入安全上下文、当前任务过滤和 30 秒限流单元测试。
- 增加损坏 rollout JSONL 的恢复回归：单行 JSON 损坏时继续跳过该行并恢复后续有效图片产物，避免历史文件局部损坏阻断任务附件读取。
- 对话顶部现在持续显示当前任务的工作目录名称，并在悬停或辅助技术标签中保留完整路径，减少移动端切换任务后的目录上下文丢失。
- 任务收件箱支持勾选当前列表中的多个任务，批量标记已读，或在未归档/已归档列表间批量归档与恢复；批量操作逐项处理并保留失败提示。
- 失败轮次现在提供“重试此消息”按钮；点击只恢复原始用户输入到编辑框，由用户确认后重新发送，不会自动重复未知结果的请求。
- 超过 12,000 个字符的 Codex 主要回复默认折叠为有限高度，保留 Markdown、代码和文件链接渲染；用户可在原地展开或收起完整内容，流式生成结束后才启用折叠。
- 将长回复折叠阈值和展开状态策略移入独立的 `public/message-display.js`，并由 Service Worker 缓存该模块；主渲染器只负责 DOM 呈现。
- 打开任务后，对话顶部同时显示工作目录、分支、来源客户端和当前状态；完整目录与来源名称保留在悬停和辅助技术标签中。
- 已停止的对话轮次现在提供“继续此消息”，恢复原始输入到编辑框后由用户确认发送，不会自动重放中止请求。
- Codex 主要回复增加“复制回复”按钮，复制原始 Markdown 文本，不受长回复折叠和展示样式影响，并保留可访问名称。
- 任务列表中的相对活动时间会在页面停留期间定期刷新，悬停显示的绝对时间也同步更新，避免“刚刚/几分钟前”长期停留在旧值。
- 对话消息和任务列表时间使用标准 `<time datetime>` 语义，辅助技术和浏览器工具可以读取规范化的实际时间。
- `/api/status` 和任务详情现在显示文件授权范围；当 `CODEX_PWA_ROOTS` 覆盖整个用户 home 时会明确给出收紧到项目目录的建议，但不会未经确认改变现有生产授权。
- 前端时间和授权诊断改动同步递增 Service Worker 缓存版本，已安装的 PWA 会获取最新资源。
- 未知 app-server 通知统计现在以有界摘要持久化到当前浏览器；只保留方法名、次数和最近时间，不保存通知正文，刷新后仍可用于协议诊断。
- SSE 最新事件游标现在保存在当前浏览器会话中；页面刷新后会尝试短窗口回放，若服务端回放窗口已重置或缺口仍会执行完整状态同步。
- 服务端现在按任务串行排队来自同一 Web UI 的发送、追加和中止请求，减少快速重复操作造成的 app-server 写入竞争；跨客户端竞争仍返回明确的 409 冲突。
- 审批和交互式问题回答也进入同一任务写入队列，避免审批响应与中止/追加操作同时发送时互相竞争。
- 空闲任务会在当前浏览器中保存有界的滚动位置；重新打开时恢复阅读位置，正在生成的任务继续优先跟随最新输出。
- 任务收件箱新增项目筛选，可按工作目录快速缩小当前任务列表，并与状态筛选和批量操作联动。
- `npm run doctor` 现在同时报告授权根目录范围和 Service Worker 缓存版本，并在根目录覆盖整个用户 home 时给出安全建议。
- 文件权限变化或目标文件被移除时，API 错误会统一为不泄露服务器路径的可操作提示，并分别使用 403 或资源不存在语义。
- 同一任务写入队列已拆为独立的 `thread-mutation-queue.mjs` 模块，并用异步并发测试验证同任务串行、不同任务并行。
- 目录浏览增加权限变化回归覆盖；`npm run doctor` 现在会单独检查授权根目录是否仍可读和可进入，便于在权限被修改后尽早定位问题。
- 多章节 Codex 长回复增加可折叠的章节导航，手机上阅读长报告时可以直接跳到对应标题。
- 增加共享 daemon、Web UI 和 SSE 端到端断线回归：浏览器离线期间产生的通知会在带事件 ID 重连后被真实回放。
- 将消息章节导航移入独立的 `public/message-outline.js`，并同步加入 Service Worker 缓存，降低对话渲染主文件的耦合。
- `npm run doctor` 现在会读取运行中的健康接口（必要时回退到 `/api/status`），比较 Web UI 运行版本与源码版本，便于发现服务未重载或运行目录漂移。
- 将时间戳归一化、绝对/相对时间和文件修改时间格式化移入独立的 `public/time-display.js`，并增加边界单元测试。
- 将权限/不存在路径错误的安全提示和状态码归一化移入独立的 `error-utils.mjs`，并纳入 Release 文件清单和单元测试。
- 磁盘空间耗尽（`ENOSPC`/`EDQUOT`）现在返回 HTTP 507 和可操作提示，不再把底层文件路径或英文系统错误直接展示给用户。
- 常见文件系统错误（目标已存在、类型不符、符号链接异常、名称过长及未知带路径错误）现在统一为安全中文提示，避免继续泄露服务器路径。
- 增加共享 daemon 重启回归：Web UI 进程停止并在同一端口重新启动后，会重新连接原 daemon，daemon 本身保持运行。
- 移动端 Chrome 回归新增切后台覆盖，确认页面隐藏时会关闭浮动菜单，回到前台不会残留遮挡层。
- 任务收件箱增加本机标签：可在任务卡片中编辑标签、按标签筛选并持久化到当前浏览器；界面明确提示标签不会同步到其他设备。
- 移动端 Chrome 回归现在真实编辑并筛选本机标签，验证标签保存、任务卡片展示和筛选结果。
- 健康接口现在同时返回 Web UI 版本；`doctor` 在认证开启时也能通过无需登录的健康检查完成运行版本对比。

## v0.18.15 — 2026-09-09

### 修复

- 安装器在私网部署完成后明确输出对应的 UFW 放行命令；
- README 和组内部署说明补充私网端口防火墙要求及“本机健康但手机无法连接”的排障步骤；
- 增加安装输出中防火墙提示的回归测试。

## v0.18.14 — 2026-09-09

### 修复

- 修复安装器将 `WorkingDirectory` 和 `EnvironmentFile` 写成带引号路径、导致 systemd 拒绝工作目录或忽略环境文件的问题；
- 安装器现在会在重启 Web UI 前使用 `systemd-analyze verify` 校验生成的用户服务配置，避免留下端口已监听但后端未正确启动的半成品部署；
- 重复安装时显式指定当前实例正在使用的端口不再被误判为端口冲突；
- 修正重装时私网 socket 与代理服务的停启顺序，避免代理被提前激活后导致 socket 重启失败；
- 增加含空格配置路径的安装回归测试，确保自定义端口和 `CODEX_BIN` 等设置可以被服务正常加载。

## v0.18.13 — 2026-09-08

### 文档

- 补充 API Key 与 Codex CLI 认证关系、`--dry-run` 和用户级服务排障命令；
- 修正任意安装目录下的维护示例，增加 Release ZIP 的 SHA-256 校验步骤；
- 说明 HTTPS 反向代理的 SSE 长连接要求，以及手机端与 Windows Codex Remote 共享任务时的单一活动写入者限制；
- 修正 GitHub Actions 自动创建 Release 的发布流程说明。

## v0.18.12 — 2026-09-08

### 文档

- 公开仓库和组内部署说明改为使用当前公开清洗镜像；
- 增加 AI 辅助部署的前提、权限边界、端口/私网网址和排障说明；
- 补充公开代码与服务器访问权限、Linux 账号隔离以及首次登录流程的安全边界。

## v0.18.11 — 2026-09-07

### 修复

- 递增 Service Worker 缓存版本，确保已安装的 PWA 能及时取得用户名/密码管理界面的新资源。

## v0.18.10 — 2026-09-07

### 新增

- 登录页和“已登录设备管理”新增“修改用户名或密码”入口；修改前必须验证当前凭据，成功后撤销所有可信设备并要求重新登录；
- 用户名独立保存于 `access-username`，旧部署没有该文件时继续兼容默认用户名 `codex`。

## v0.18.9 — 2026-09-07

### 修复

- 将 GitHub 托管运行器上的 Chrome 调试端口启动等待从 6 秒延长至 30 秒；
- CI 环境使用 `--no-sandbox`、显式回环调试地址和无 GPU 参数，提高无界面 Chrome 的兼容性；
- 浏览器或测试服务器提前退出时立即失败，并在测试日志中附带受限长度的进程诊断信息；
- `check` 与 `release` 工作流运行同一测试产生的两个红叉现在可追溯到同一底层失败，而不会误判为两个产品缺陷。

## v0.18.8 — 2026-09-07

### 修复

- 移动端浏览器回归测试会等待应用外壳真正挂载，不再因 GitHub 托管运行器启动较慢而提前访问空元素；
- ZIP 更新器的旧版 systemd 配置测试会保留当前 Node.js 可执行目录，兼容 GitHub `setup-node` 不将 Node 安装到 `/usr/bin` 的环境；
- 发布检查现在可以在服务器和 GitHub Actions 的 Node.js 22 环境中得到一致结果；
- 新增面向仓库专用 Deploy Key 的隔离镜像发布脚本，只接受已清洗 ZIP，且以原子操作推送 `main` 和版本标签；
- 新标签会在 GitHub Actions 再次通过完整检查后自动生成 ZIP、clean Git bundle、SHA-256 校验文件并创建 Release。

## v0.18.7 — 2026-09-06

首个隐私清洗后的组内发布候选版。

### 主要能力

- 手机优先的 Codex 任务列表、历史记录、实时输出和跨端平滑交接；
- 每任务模型、推理强度、权限和 Goal 状态管理；
- Markdown、数学公式、表格、Diff、图片及常见服务器文件预览；
- 服务器目录选择、文件管理、上传和下载；
- 90 天可信设备、设备管理、断网重连和离线外壳；
- 分用户 systemd 安装、诊断、原子 ZIP 更新和兼容卸载。

### 本版修复

- 保留任务实际记录的模型，即使该模型尚未出现在 app-server 模型目录中；
- `gpt-6-astra` 等目录外模型不再被错误显示成默认模型；
- 兼容任务设置位于 `thread/read`、`thread/resume` 顶层或嵌套字段的响应；
- 归档任务或实时订阅暂时失败时仍显示其已记录模型和推理强度；
- 目录外模型的推理强度继续使用统一的中英双语标签。

### 发布验证

- Node.js 语法检查通过；
- 自动化测试 62/62 通过；
- `npm audit --omit=dev` 报告 0 个已知漏洞；
- 干净发布包不包含旧 Git 历史、运行凭据、Codex 会话、依赖目录或个人工作文件。
- 补齐任务范围和任务详情 Tab 的可访问语义：关联内容面板、维护 roving `tabindex` 与 `aria-hidden` 状态，并支持键盘方向键/Home/End 切换；通用键盘导航移入 `public/tab-navigation.js`，新增静态无障碍回归测试。Service Worker 更新到 v85。
- 将 SSE 连接、heartbeat、事件回放恢复、去重和后台请求分发移入 `public/event-connection.js`，保持原有恢复边界和设备隔离行为；Service Worker 更新到 v86。
- 将 Goal 读取、状态变更、清除、编辑和顶部 Goal 条渲染移入 `public/goal-actions.js`，保留确认、加载提示和任务状态同步；Service Worker 更新到 v87。
- 将线程列表、详情、历史分页、运行记录、输出和制品读取路径集中到 `public/thread-api.js`，统一任务 ID 编码和查询参数序列化；新增 API 路径回归测试，Service Worker 更新到 v88。
- 将历史节点列表、分页加载、搜索、虚拟滚动和定位状态移入 `public/history-nodes.js`，主应用继续注入历史上下文定位能力；保持历史节点缓存上限、加载状态和无障碍 `aria-busy` 语义，Service Worker 更新到 v89。
- 将历史上下文横幅、上下文窗口虚拟滚动、前后边界和返回最新对话视图移入 `public/history-context.js`，主应用继续注入历史加载与渲染依赖；Service Worker 更新到 v90。
